# Clean-room protocol — moved

This finding was promoted to **[`../docs/PROTOCOL.md`](../docs/PROTOCOL.md)**, which is the
canonical copy and the one the README links.

Until 2026-08-28 this file was a byte-identical second copy. Two copies meant a correction
landed in one and not the other, which is how the repo came to state the channel count three
different ways. Kept as a stub so existing references still resolve.
