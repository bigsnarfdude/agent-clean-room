# MISTAKES

- agent1: exp007 — Tried scipy with solver_tol=1e-13, n_nodes=300. CRASH: "maximum number of mesh nodes exceeded". The adaptive refinement in solve_bvp hits its max_nodes=5000 limit at tight tolerances. Lesson: scipy can't go below ~1e-11 for this problem; switch to Fourier.
- agent1: exp009 — Tried scipy solver_tol=1e-12 with n_nodes=198. Same crash. Lesson: Even moderate mesh with tight tolerance crashes scipy.
- agent1: exp013 — Tried Fourier 128 modes. Took 30 seconds (timeout risk) and stalled at 5e-12. Lesson: More modes = larger Jacobian = slower convergence, not better accuracy for this smooth problem.
- agent1: exp017/exp022/exp024 — Various "almost converged" Fourier runs where newton_tol was too tight for the given mode count. Newton stalls in max-norm well above 1e-14 with too many modes. Lesson: Match mode count to problem complexity (4 modes suffice for cos forcing).
- agent0: exp008 — scipy n_nodes=300 + solver_tol=1e-12 crashes same as agent1. Lesson: scipy adaptive mesh exceeds max_nodes=5000 at tight tolerances.
- agent0: exp010/016/018/020 — Fourier N=64/96 with tight newton_tol: Newton stalls at 1e-12 to 5e-13 max-norm, never converging. Lesson: Large Jacobians accumulate roundoff, preventing quadratic convergence below ~1e-13.
- agent0: exp041-044 — Tried large-amplitude, mode-2, and step-like ICs to find a 4th solution branch. All converge to known ±1 branches. Lesson: For K_amplitude=0.3, there are exactly 3 branches (trivial, ±1). No heteroclinic or higher-mode solutions exist.
- agent0: exp073 — Fourier solver for trivial branch gives 4.5e-15, WORSE than scipy's exact 0.0. Lesson: For the exact trivial solution u≡0, scipy is better because no Newton iteration roundoff. Fourier only beats scipy for non-trivial branches.
- agent1: Spent many experiments on heteroclinic branch search (exp043-051) before agent0 confirmed no 4th branch exists for K_amplitude=0.3. Lesson: Check blackboard for other agents' results before launching expensive search campaigns.
- agent1: Basin boundary binary search (exp102-149) finds DIFFERENT boundaries at different amplitudes. The "main boundary" is not a single number — it depends on all IC parameters. Lesson: Basin boundary is a function of (u_offset, amplitude, n_mode, phase), not just u_offset alone.
- agent0+agent1: ★ CRITICAL ★ Both agents claimed N=4 Fourier was optimal with "machine precision" 1.96e-16 residuals. This was WRONG. The reported residual is the ALIASED solver-grid (M=8) value. The TRUE PDE residual on a 500-point fine grid is 1.42e-07 for N=4 — the WORST of any N tested. The code takes min(solver_grid, fine_grid) which picks the aliased value. True optimal is N≥8 (true residual ~5e-12). The "50,000x improvement over scipy" claim was false — the true improvement is 7.4x. Lesson: ALWAYS verify that residual metrics are computed on an independent evaluation grid, not the solver grid where aliasing makes the residual artificially small.
